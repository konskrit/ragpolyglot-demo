# document-service.Tests

> **WIP** — xUnit tests for document-service.

Covers embedded SQL load, poison-message classification, status SQL, ingest retry policy, maintenance settings, and progress stages.

```bash
npx nx test document-service-tests
```
