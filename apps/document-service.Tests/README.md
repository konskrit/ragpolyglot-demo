# document-service.Tests

xUnit tests for document-service: embedded SQL load, poison-message classification, status SQL, ingest retry policy, and maintenance settings.

```bash
npx nx test document-service-tests
```
